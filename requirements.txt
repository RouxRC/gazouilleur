lxml
feedparser
twisted
pyOpenSSL
pymongo

# Twitter: Switch preferably to personal branch fixing POST for Twitter Streaming API until v1.10.1 with pull request released
# To reinstall cleanly, do within the virtualenv:
# `pip uninstall twitter; git clone git+https://github.com/RouxRC/twitter.git; cd twitter; python setup.py build; python setup.py install`
#twitter>=1.10.1
git+https://github.com/RouxRC/twitter.git#twitter-1.10.0.fork-post

# PyPump (for Identi.ca): Waiting for a stable release post 0.3 with api changes <https://github.com/xray7224/PyPump/pull/63>
pypump==0.3.0
#git+https://github.com/xray7224/PyPump.git

# Colifrapy: Hey @Yomguithereal, told you I was gonna use it sometime! :)
colifrapy==0.4.0

# The following are only required to use the URL_STATS functionality. You can comment them if you don't need it
# numpy apparently needs to be installed before separately for matplotlib to be installed properly, so preferably run first:
# pip install numpy
matplotlib
pystache
